# 🔒 stego-cipher  
**Hide encrypted messages in images using LSB steganography**

![Python](https://img.shields.io/badge/Python-3.8%2B-blue?logo=python)
![License](https://img.shields.io/badge/License-MIT-green)
![Platform](https://img.shields.io/badge/Platform-Cross--Platform-lightgrey)

A secure, easy-to-use Python tool that **encrypts and hides secret messages inside any image** (JPG, PNG, BMP, TIFF, etc.) using **Least Significant Bit (LSB) steganography**. Output is always saved as a **lossless PNG** to preserve hidden data.

Built with `cryptography.fernet` (AES-128) and `Pillow` — no unnecessary dependencies.

---

## 📁 Project Structure

After cloning, your folder will look like this:

```markdown
stego-cipher/
├── input_images/       ← Place your cover images here (any format: JPG, PNG, BMP, etc.)
├── output_stego/       ← Stego images saved here (always as PNG)
├── crypto_utils.py     ← Encryption/decryption logic (Fernet AES-128)
├── stego_engine.py     ← LSB embedding/extraction engine
├── main.py             ← CLI menu interface (Hide / Reveal)
├── requirements.txt    ← Python dependencies (Pillow, cryptography)
├── .gitignore          ← Excludes secret.key, user folders, cache files
└── secret.key          ← Auto-generated on first run — NEVER commit this!
```

> ⚠️ **Never commit `secret.key`** — it’s your encryption key! It’s automatically ignored by `.gitignore`.

---

## 🚀 Quick Start

```bash
# Clone the repository
git clone https://github.com/Divyardhansinh/stego-cipher.git
cd stego-cipher

# Set up virtual environment (recommended)
python -m venv venv
source venv/bin/activate    # Linux/macOS
# venv\Scripts\activate     # Windows

# Install dependencies
pip install -r requirements.txt

# Run the tool
python main.py
```

---

## 🧪 How to Use

### ► **Hide a message**
1. Put your image (e.g., `photo.jpg`, `art.png`, `scan.bmp`) into `input_images/`.
2. Run: `python main.py`
3. Choose **Option 1**.
4. Enter filename: `photo.jpg`
5. Enter your secret message.
6. Output saved as: `output_stego/stego_photo.png`

### ► **Reveal a message**
1. Ensure the stego image (e.g., `stego_photo.png`) is in `output_stego/`.
2. Run: `python main.py`
3. Choose **Option 2**.
4. Enter filename: `stego_photo.png`
5. View your **decrypted secret message**!

> 💡 **Important**: Only reveal from **PNG files generated by this tool**. Original JPEGs/BMPs **cannot** contain hidden data due to compression.

---

## 🔐 Security & Keys

- 🔑 On first run, the tool auto-generates `secret.key` — your **AES encryption key**.
- 🔒 All messages are **encrypted before being hidden** — no plaintext is ever stored in the image.
- 🗑️ If you delete `secret.key`, a new one is created — **old messages become permanently undecryptable**.
- ❌ **Never share, email, or commit `secret.key`** — treat it like a password.

---

## 📦 Dependencies

| Package | Purpose |
|--------|--------|
| `Pillow >=10.0.0` | Load, convert, and manipulate images |
| `cryptography >=41.0.0` | Secure AES-128 encryption/decryption |

Install with:
```bash
pip install -r requirements.txt
```

---

## 🛑 Common Issues & Fixes

### ❌ “Image not found” error (macOS)
- **Cause**: Copying filenames from Finder inserts invisible Unicode spaces.
- **Fix**: Drag the file into the terminal when prompted, or type the filename manually.

### ❌ “No hidden message found”
- **Cause**: You’re trying to reveal from a non-stego image or original format (e.g., JPEG).
- **Fix**: Only use PNGs created by **Option 1** of this tool.

### ❌ `ModuleNotFoundError: No module named 'numpy'`
- **Cause**: Old code included unused `import numpy`.
- **Fix**: Ensure you’re using the latest `stego_engine.py` — **NumPy is not required**.

---

## 📜 License

MIT License — free to use, modify, and distribute for personal or commercial projects.

---

## 👤 Author

Made by **[Divyardhansinh](https://github.com/Divyardhansinh)**  
For **educational, ethical, and creative use only**.
Your GitHub repo will now look **professional, clear, and inviting** — perfect for your portfolio or sharing with others! 🎉

Let me know if you'd like a **dark-mode optimized version** or **screenshots added**!
